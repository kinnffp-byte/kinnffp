# 김쁘피 여름 보상 아카이브 — 사용 설명서

`kimfp.pages.dev` 용 **정적 사이트**입니다. 빌드 과정이 없어서 파일을 그대로 올리면 바로 뜹니다.

---

## 1. 배포 (Cloudflare Pages)

이전에 안 켜졌던 이유는 **Next.js 소스를 그대로 올렸기 때문**입니다. 그 프로젝트는
`index.html`이 없고 서버(Worker)가 매 요청마다 HTML을 만들어내는 구조라, Pages가
정적 서빙하면 열 파일이 없어서 빈 화면이 떴습니다. 이 폴더는 그 문제가 없습니다.

1. GitHub 저장소에 **이 폴더 안의 내용을 그대로** 올립니다 (`index.html`이 최상단에 오도록).
2. Cloudflare Pages → 해당 저장소 연결
3. **Framework preset: `None`**, **Build command: 비움**, **Build output directory: 비움(또는 `/`)**
4. Deploy

재배포는 바뀐 파일만 덮어쓰고 Commit → 1~2분 → **Ctrl+Shift+R** (안 되면 시크릿 창).

```
index.html      메인 (5개 화면 전부 이 한 파일)
admin/          관리자 (index.html + admin.js)
supabase_전체.sql  DB 설치 SQL — 한 번만 실행
app.js          화면 전환·렌더링·다크모드·확대뷰어
style.css       스타일 (팔레트는 :root 한 곳)
supabase.js     DB 연결 — 아래 2번 참고
fx.js           로딩 게이트
og.jpg          카톡·X 링크 미리보기 썸네일
assets/         이미지 (poster/ 안은 의상 포스터용 3:4 크롭)
```

> **DB를 연결하지 않아도 사이트는 정상 동작합니다.** 연결 전에는 `index.html`에 들어있는
> 기본값으로 표시되고, 연결하면 DB 값이 그 위를 덮어씁니다.

---

## 2. Supabase 연결 (관리자 페이지 쓰려면 필요)

`supabase.js` 맨 위 두 줄만 채웁니다.

```js
const SUPABASE_URL  = 'https://bpzwdbnglicqmhqtdpiu.supabase.co';
const SUPABASE_ANON = 'eyJhbGciOi…(설정 완료)';
```

- Supabase → New project → Settings → API 에서 **Project ID**와 **anon public 키** 복사
- ⚠️ **한 프로젝트 = 한 사람.** 다른 사람 프로젝트를 재사용하면 옛 데이터가 남습니다.
- Supabase → SQL Editor → 새 쿼리 → **`supabase_전체.sql` 전체를 붙여넣고 Run** (한 번에)
- 마지막에 확인 쿼리가 돌아갑니다. 기대값: profile 1 / promises 10 / fixed_rewards 5 /
  roulette 18 / goods_tiers 6 / merch 12 / outfits 10

---

## 3. 관리자 페이지

`배포주소/admin/` 으로 들어갑니다. **먼저 비밀번호를 정해야 합니다.**

`admin/index.html` 에서 이 줄을 찾아 고치세요.

```js
var ADMIN_PW = "1234";   ← 소스에 보이니 반드시 버리는 비번으로
```

> ⚠️ 비밀번호는 소스에 그대로 보입니다. **버리는 비번**을 쓰세요.
> anon 키로 쓰기 때문에, 키가 노출되면 비번과 무관하게 수정이 가능합니다.

### 탭 구성

| 탭 | 고칠 수 있는 것 |
|---|---|
| 🏠 사이트 | 제목·설명·로고·상태바·사이드바 메모·푸터·전환 연출 |
| 🧭 목차 | 5칸의 기호·영문 라벨·이름 (사이드바·독·모바일 동시 적용) |
| 📄 메인 | 히어로 전체·지표 4칸·송출 표시·목차 카드 3개·하단 블록 제목 |
| 📈 누적공약 | 머리글·진행률(현재/목표)·하단 안내 + **공약 목록 표** |
| 🎲 방셀보상 | 두 패널 제목·배지·열 나누기 + **확정 단가 표**·**룰렛 표** |
| 🎁 굿즈 | 머리글·제외 안내·갤러리 제목 + **굿즈 단계 표**·**갤러리 표** |
| 👗 의상 | 머리글·컬렉션 2종(대표사진·이름·설명) + **포스터 표** |
| 🎨 테마 | 색 13종 · 컬러피커 + hex 입력 · 즉시 미리보기 · 기본값 복원 |

### 표 다루기
- **+ 줄 추가** → 맨 아래에 빈 줄이 생깁니다.
- **▲▼** 로 순서를 바꿉니다. 사이트에는 이 순서대로 나옵니다.
- **×** 로 지웁니다.
- 각 표마다 **저장** 버튼을 따로 눌러야 반영됩니다. (문구는 탭 아래 "이 탭 문구 저장")
- 굿즈 단계의 **사진 주소**는 한 줄에 하나씩 여러 장 넣습니다.
- 의상의 **컬렉션**이 `summer` = 왼쪽 사진, `ppugini` = 오른쪽 사진을 눌렀을 때 나옵니다.

### 진행률
누적공약 탭의 **현재 누적 / 최종 목표** 두 숫자로 자동 계산됩니다.
공약 금액(`10만`)에서 숫자만 뽑아 비교해서, 달성한 칸은 진하게 / 다음 목표는 포인트 색으로 표시됩니다.

---

## 4. 화면 구성

| 목차 | 내용 |
|---|---|
| 메인 | 히어로 + 목차 카드 3개 + 의상 2종 미리보기 + 확정 방셀 단가 + 굿즈 소개 |
| 누적공약 | 진행률 게이지 + 공약 10개 로드맵 (가로 스크롤) |
| 방셀보상 | 확정 방셀 단가 5종 + 100개 방셀룰렛 18항목 |
| 굿즈 | 단계별 6종 + 제외 안내 + 전체 굿즈 갤러리 12종 |
| 의상 | 컬렉션 2종(사진 클릭) + 포스터 5종 격자 + 확대 뷰어 |

### 조작
- **왼쪽/오른쪽 의상 사진을 누르면** 그 컬렉션의 포스터 5종이 아래에 펼쳐집니다.
- 포스터를 누르면 확대 뷰어 — **← →** 이동, **ESC** 닫기, "원본 열기"로 4K 원본.
- 사이드바 맨 아래 **NIGHT** 버튼 = 다크모드 (선택은 브라우저에 저장됩니다).
- 일반 이미지(굿즈 등)를 누르면 확대됩니다.

---

## 5. 색 바꾸기

보통은 **관리자 🎨 테마 탭**에서 바꾸면 됩니다 (13색, 저장하면 전 화면 적용).
직접 고치려면 `style.css` 맨 위 `:root` **한 곳만** 손대면 됩니다.

```css
:root {
  --ink: #11131a;        /* 본문 글자 */
  --paper: #f7f7f3;      /* 시트 배경 */
  --night: #131620;      /* 사이드바·전환 커버 */
  --iris: #737de8;       /* 포인트 색 (킥커·번호·배지) */
  --ice: #b8e9ef;
  ...
}
```

> 원래 CSS에는 팔레트가 **여러 겹으로 중복 정의**돼 있어서 `:root`를 고쳐도 안 먹었습니다.
> 지금은 뒤쪽 중복 선언 25개를 정리해서 `:root`가 유일한 출처입니다.

---

## 6. 링크 미리보기(카톡·X 썸네일)

크롤러는 JS를 안 돌리므로 **DB가 아니라 `index.html` 상단 메타 태그**를 읽습니다.
관리자에서 제목을 바꿔도 미리보기는 안 바뀝니다.

```html
<meta property="og:title" content="김쁘피 여름 보상 아카이브">
<meta property="og:image" content="og.jpg">
```

- 이미지를 바꿀 땐 **파일명도 같이 바꾸세요** (`og.jpg` → `og-v2.jpg`). 같은 이름이면 캐시됩니다.
- 권장 1200×630, 1MB 이하.
- 카카오 캐시 초기화: `https://developers.kakao.com/tool/debugger/sharing`

---

## 7. 문제 빠른 참조

| 증상 | 원인 / 해결 |
|---|---|
| 배포했는데 빈 화면 | `index.html`이 저장소 최상단에 있는지 / Build command를 비웠는지 확인 |
| 바꿨는데 안 바뀜 | 캐시 → Ctrl+Shift+R, 시크릿 창 |
| 색을 바꿨는데 일부만 | 그 요소가 팔레트 변수를 안 쓰는 경우 — 어느 요소인지 알려주면 변수로 바꿔줌 |
| 이미지가 안 뜸 | `assets/` 폴더도 같이 올렸는지 확인 |
| 다크모드가 이동마다 풀림 | `localStorage('theme')` 저장 — 시크릿 창에서는 유지되지 않음 |
| 관리자에서 저장했는데 반영 X | `supabase.js` 두 줄 + `supabase_전체.sql` 실행 여부 확인 |
| 관리자에 "DB에 연결되지 않았습니다" | `supabase.js` 두 줄이 아직 `{{ }}` 자리표시자입니다 |
| 관리자 로그인이 안 됨 | `admin/index.html` 의 `ADMIN_PW` 를 아직 안 정했습니다 |
| 표에 저장했는데 사이트에 순서가 다름 | ▲▼ 로 정한 순서가 저장됩니다 — 저장 버튼을 눌렀는지 확인 |
| 굿즈 사진이 한 장만 뜸 | 사진 주소를 **줄바꿈**으로 나눠 넣어야 합니다 (쉼표 아님) |
---

## 7. SOOP 프사 연동

`admin → 🏠 사이트 → 📺 SOOP 연동` 에서 **아이디만** 넣으면 됩니다. (현재 `gurm01`)

주소는 이 규칙으로 자동 조립됩니다:
```
https://profile.img.sooplive.co.kr/LOGO/{앞2글자}/{아이디}/{아이디}.jpg
gurm01 → .../LOGO/gu/gurm01/gurm01.jpg
```

아이디 하나로 따라오는 것들:
- **브라우저 탭 아이콘(파비콘)** — 주소창에 뜨는 작은 이미지
- **화면 전환 커버 이미지** — 목차 누를 때 덮이는 화면 가운데
- **관리자 페이지 탭 아이콘**

**다른 이미지를 쓰고 싶을 때**만 아래 `커버 이미지` 칸을 채우세요. 채우면 그게 우선하고,
비우면 다시 SOOP 프사로 돌아갑니다. 움짤(.gif)도 됩니다.

`방송국 주소` 칸은 푸터의 **SOOP 방송국 ↗** 링크로 갑니다. 비우면 링크가 숨겨집니다.

> ⚠️ 카톡·X 미리보기 썸네일은 여기서 안 바뀝니다. 크롤러는 JS를 안 돌려서
> `og.jpg` 파일을 그대로 읽습니다 (5번 항목 참고).

---

## 8. 알아두면 좋은 구조

**관리자 화면 색 ≠ 사이트 색.** 관리자 UI는 `--adm-*` 변수를 따로 씁니다.
예전에는 둘이 `--paper` 같은 이름을 공유해서, 🎨테마를 저장하면 **관리자 배경까지
사이트 색으로 튀는 문제**가 있었습니다. 지금은 분리돼 있어서 테마를 아무리 바꿔도
관리자 화면은 그대로입니다. 바뀐 색은 테마 탭 오른쪽 **미리보기 패널**에서 확인하세요.

테마가 실제로 칠하는 곳:
`--paper`(목차 카드 띠) · `--sheet`(본문 시트) · `--hero`(히어로 배경) ·
`--night`(사이드바) · `--iris`(포인트) · `--ink`(글자) 등 13종.

---

## 9. 주소(URL) 구조

5개 화면이 `index.html` 하나에 들어 있고, **어느 화면인지는 주소 뒤 `#`로 구분**합니다.

```
kimfp.pages.dev/            → 메인
kimfp.pages.dev/#promise    → 누적공약
kimfp.pages.dev/#reward     → 방셀보상
kimfp.pages.dev/#goods      → 굿즈
kimfp.pages.dev/#outfit     → 의상
```

이렇게 되어서 가능한 것들:
- **새로고침해도 그 화면에 그대로 있습니다.** (예전에는 항상 메인으로 튀었습니다)
- 브라우저 **뒤로/앞으로** 버튼이 화면 이동으로 동작합니다.
- **특정 화면 링크를 바로 공유**할 수 있습니다. 예: 의상만 보여주고 싶으면 `…/#outfit`
- SOOP 게시글 iframe에도 `src="…/#outfit"` 처럼 특정 화면을 바로 띄울 수 있습니다.

없는 이름(`#zzz`)을 넣으면 메인으로 갑니다.

> 폴더로 나누는 방식(`/outfit/index.html`)도 가능하지만, 그러면 화면마다 파일이 복사돼서
> 문구 한 줄 고칠 때 5곳을 손봐야 합니다. 지금 구조는 파일 하나 · 관리자 한 벌로 끝납니다.
> `/outfit` 처럼 `#` 없는 주소를 원하면 말씀해 주세요 (`_redirects` 설정을 추가하면 됩니다).

---

## 10. 아이패드 목업 하단

예전에 좌측 하단에 **12px 크림색 띠**가 보였습니다. 사이드바 높이가
`calc(100svh - 130px)` 즉 **브라우저 창 높이 기준**이었는데, 사이드바는 실제로는
아이패드 목업 내부(스크롤 영역) 안에 있어서 두 값이 애초에 일치할 수 없었습니다.
창 크기를 바꾸면 틈 두께도 계속 달라졌습니다.

지금은 컨테이너 쿼리(`container-type: size` + `height: 100cqh`)로 **스크롤 영역의
실제 높이를 참조**합니다. 창 크기와 무관하게 항상 딱 맞습니다.

사이드바 내용이 화면보다 길어지면(작은 창) 사이드바 자체가 스크롤되며,
스크롤바는 숨겨져 있습니다.

**하단 독(dock)은 제거했습니다.** 원래 코드에 `display:none`으로 꺼둔 채
마크업과 CSS 70여 줄이 남아 있었습니다(주석: *"Kept in the component for a future
restore"*). 사이드바에 이미 목차 5칸이 다 있어 중복이라 지웠습니다.
아이패드 하단 홈 인디케이터(얇은 막대)는 그대로 둡니다.

---

## 11. 창 뒷배경 (전에꺼가 계속 뜨던 문제)

원인은 `.view-experience` 가 **불투명 회색 `#d7d9dc`** 로 화면 전체를 덮고 있었던 것입니다.
그 아래 `body` 가 무슨 색이든, 테마를 어떻게 바꾸든 회색만 보였습니다.
그 회색은 예전 디자인 단계에서 남은 값입니다.

지금은 덮개를 걷고 뒷배경을 테마에 연결했습니다.

**🎨테마 탭에 색 2개가 추가되었습니다.**

| 항목 | 설명 |
|---|---|
| 창 뒷배경 | 아이패드 목업 **바깥쪽** 넓은 영역 |
| 화면 바닥 | 아이패드 화면 자체의 바닥색 (상태바도 따라감) |

**🏠사이트 탭에 “창 뒷배경 이미지”** 칸도 있습니다. 비우면 위의 색만,
채우면 그 그림이 목업 뒤에 고정되어 깔립니다. (`assets/kimppugi-summer-outer.png`
같은 값. 원래 이 그림을 깔도록 되어 있었는데 회색 덮개에 가려 안 보였습니다.)

다크모드에서는 뒷배경·화면 바닥이 자동으로 어두워집니다.

> **SQL:** 새 키가 3개 늘었습니다(`theme-page`·`theme-screen`·`page-image`).
> 전체 재실행하지 말고 파일 맨 아래 **[추가 패치 2]** 블록만 Run 하세요.

---

## 12. 덮어써도 관리자 수정분이 안 날아가게

### 파일을 다시 올리는 건 안전합니다
문구·순서·이름·색은 전부 **DB(Supabase)** 에 있습니다. `index.html`·`app.js` 같은
파일을 덮어써도 DB는 그대로이므로 관리자에서 고친 내용은 유지됩니다.

### 위험한 건 SQL 재실행이었습니다 — 이제 막았습니다
예전 SQL은 맨 위에 `drop table` 이 있고 profile 을 `do update` 로 덮어써서,
다시 돌리면 관리자 수정분이 전부 초기화됐습니다. 지금은 이렇게 바뀌었습니다.

| | 예전 | 지금 |
|---|---|---|
| 테이블 | `drop table` 후 재생성 | `create table if not exists` (있으면 그대로) |
| profile | `on conflict do update` (덮어씀) | `on conflict do nothing` (보존) |
| 목록 시드 | 무조건 insert | `where not exists` — **비어 있을 때만** |

**이제 전체 SQL을 몇 번 돌려도 기존 데이터를 건드리지 않습니다.**
정말 초기화하고 싶을 때만 파일 맨 위 `-- drop table …` 7줄의 주석을 지우고 돌리세요.

### 기본값도 실제 순서로 맞춰뒀습니다
혹시 DB를 새로 깔거나 비우게 되더라도, 아래 순서·이름으로 다시 세팅됩니다.

| 컬렉션 | 순서 |
|---|---|
| 여름 한정 의상 | 삼국지 초선 한정(02) · 여름 A(10) · B(08) · C(01) · D(07) |
| 쁘키니 한정 의상 | 쁘키니 히든(03) · A(04) · B(06) · C(05) · D(09) |

> 괄호 안은 `summer-pose-NN` 파일 번호입니다.

### 이름 표기
**쁘키니**로 통일했습니다(예전에 잘못 `뿌기니`로 들어가 있던 29곳 수정 — 의상·룰렛·방셀
단가·굿즈 전부). 영문 배지도 `PPUGINI` → `PPEUKINI` 로 맞췄습니다.
다른 표기를 쓰고 싶으면 `admin → 👗의상 → 쁘키니 한정 의상`의 **영문 라벨** 칸에서
바꾸면 배지까지 같이 따라갑니다.

### 포스터 타일 숫자
`01 / 02` 처럼 분수처럼 보여 헷갈렸던 걸 **`01` `02` `03` `04` `05`** 로 정리했습니다.
컬렉션 번호는 위쪽 탭이 이미 알려주므로 타일에서는 뺐습니다.

---

## 13. 이름 '쁘'로 통일 + 모바일 세로형

### 13-1. `뿌` → `쁘` 전부

파일 쪽은 전부 정리했습니다. **DB에 이미 저장된 값은 SQL 패치를 한 번 돌려야** 바뀝니다.
(SQL은 기존 데이터를 보존하도록 만들어져 있어서, 오타도 같이 보존되고 있었습니다.)

`supabase_전체.sql` 맨 아래 **[추가 패치 3] · [3-2] · [4]** 세 블록을 긁어서 Run 하세요.
여러 번 돌려도 안전하고, 각 블록 끝의 확인용 `select` 결과가 **전부 0이면 완료**입니다.

| 패치 | 내용 |
|---|---|
| 3 | 뿌기니 → 쁘키니 (문구·의상·룰렛·단가·굿즈 전부) |
| 3-2 | 내부 컬렉션 키 `ppugini` → `ppeukini`, 커버 파일명 |
| 4 | 룰렛 `뿌`→`쁘`, `쿼뿌`→`쿼쁘`, `뿌독`→`쁘독` |

**패치를 안 돌려도 사이트는 안 깨집니다.** 구 컬렉션 키(`ppugini`)와 구 파일명
(`ppugini-outfits.png`)을 계속 인식하도록 만들어 뒀습니다. 다만 이름 글자는 DB 값을
그대로 보여주므로, 글자를 바꾸려면 패치가 필요합니다.

> 참고: `assets/ppugini-outfits.png` 는 새 이름(`ppeukini-outfits.png`)으로 복사해 두고
> 구 파일도 남겼습니다. 패치 3-2를 돌린 뒤 구 파일은 지워도 됩니다.
> 안 쓰이던 `ppugini-hidden-cutout.png` 는 삭제했습니다.

### 13-2. 모바일에서 세로형 아이패드

가로형(4:3) 목업을 폰에 넣으면 화면 높이의 25% 정도만 쓰고 아주 작게 눌렸습니다.
**700px 이하에서는 세로형(3:4)** 으로 세우고, 사이드바를 **상단 가로 목차줄**로 눕혔습니다.

| | 이전 | 지금 |
|---|---|---|
| 비율 | 4:3 가로 | **3:4 세로** |
| 크기(390×844) | 374×280 | **378×504** (화면 높이 60%) |
| 목차 | 왼쪽 세로 사이드바(128px) | **상단 가로 5칸** |
| 본문 폭 | 약 200px | **352px** |

좁은 폭에서는 목차의 기호·영문 라벨·하단 메모를 접고, 다크 토글은 우상단 아이콘으로
옮겨 공간을 확보했습니다. **데스크탑·태블릿은 그대로 가로형(4:3)** 입니다.

### 13-3. 그리고 위아래로 꽉 채웠습니다

3:4 비율을 지키면 폭이 이미 화면 최대라 높이가 60%에서 멈춥니다.
폰에서는 **비율 고정을 풀고 화면 높이에 맞춰 늘렸습니다.**

| 기기 | 목업 크기 | 화면 높이 사용 |
|---|---|---|
| 390×844 | 378×832 | **99%** |
| 360×780 | 348×768 | **98%** |
| 430×932 | 418×920 | **99%** |
| 390×690 (주소창 있을 때) | 378×678 | **98%** |

주소창이 보이는 실제 브라우저에서는 `100svh` 가 작아지므로 비율이 태블릿에 가깝고,
전체화면일수록 세로로 길어집니다. 어느 경우든 여백 없이 꽉 찹니다.

---

## 14. 모바일 컴팩트 — 한 화면에 더 많이

세로로 늘려도 내용이 커서 스크롤이 길었습니다. 폰(700px 이하)에서 본문을 압축해
**스크롤 길이를 37% 줄였습니다.**

| 화면 | 이전 | 지금 | 감소 | 화면 수 |
|---|---|---|---|---|
| 메인 | 3048px | 2261px | −26% | 3.9 → 2.9 |
| 누적공약 | 937px | 876px | −7% | 1.2 → 1.1 |
| 방셀보상 | 1905px | **1046px** | **−45%** | 2.4 → 1.3 |
| 굿즈 | 3906px | **2194px** | **−44%** | 5.0 → 2.8 |
| 의상 | 2421px | **1357px** | **−44%** | 3.1 → 1.7 |
| **합계** | 12,217px | **7,734px** | **−37%** | |

### 무엇을 줄였나

**열 수를 늘린 것** (효과가 가장 큼)
- 룰렛 18항목: 1열 → **2열** (18줄 → 9줄)
- 굿즈 6단계: 1열 → **2열** (6줄 → 3줄)
- 굿즈 갤러리 12개: 2열 → **3열** + 썸네일 칸 정사각 (6줄 → 4줄)
- 의상 컬렉션 카드 2개: 1열 → **2열** (나란히)
- 메인 의상 미리보기: 1열 → **2열**
- 포스터 격자: 첫 장 확대(2×2) 해제 → 2열 3줄

**크기·간격**
- 섹션 사이 간격 46~62px → **22px**
- 제목 `h2` → 30px, 큰 이미지 310px → **148px**
- 목록 행 높이·카드 안쪽 여백·글자 크기 전반 축소

**데스크탑·태블릿은 전혀 바뀌지 않았습니다** (700px 초과에서는 원래 레이아웃 그대로).

> 참고: 메인 화면의 의상 미리보기 캡션은 원래부터 이미지 위에 그라데이션으로 겹쳐
> 얹히는 설계입니다(데스크탑도 동일). 겹침 검사에서 잡히지만 정상입니다.

---

## 15. 로고 · 로고 톤 팔레트

### 자산
```
assets/logo-wordmark.png        KIMFP 워드마크 (하늘색+골드)
assets/logo-wordmark-white.png  같은 워드마크 흰색 — 어두운 배경용
assets/logo-emblem.png          달·물결 엠블럼
```
받은 원본에서 투명 여백을 잘라내고 가로 1200px 이하로 줄였습니다.

### 배치
| 위치 | 자산 | 관리자 칸 |
|---|---|---|
| 왼쪽 사이드바 맨 위 | 흰색 워드마크 | 🏠사이트 → **로고 (워드마크)** |
| 메인 첫 화면 우측 상단 | 엠블럼 | 🏠사이트 → **엠블럼 (달·물결)** |

두 칸 다 **비우면 원래대로** 돌아갑니다(워드마크를 비우면 “KIMFP” 글자, 엠블럼을 비우면 표시 안 함).

### 팔레트를 로고 톤으로
로고에서 뽑은 색은 하늘색 `#90d8f0`·`#90c0f0` 과 골드 `#f0c43c` 입니다.
다만 **이 색을 글자에 그대로 쓰면 흰 배경에서 대비가 1.5:1** 밖에 안 돼 읽을 수 없습니다.
그래서 톤은 유지하면서 대비를 확보한 값으로 정했습니다.

| 변수 | 이전 | 지금 | 대비 |
|---|---|---|---|
| `--ink` 본문 | `#11131a` 새까망 | `#243447` 남청 | 12.1:1 |
| `--night` 사이드바 | `#131620` 새까망 | `#2C4A63` 로고 남청 | 흰글자 9.3:1 |
| `--hero` 히어로 | `#1d2b3c` | `#2C4A63` | 흰글자 9.3:1 |
| `--iris` 포인트 | `#737de8` 보라 | `#1F6A97` 로고 하늘색 | 5.6:1 |
| `--ice` | `#b8e9ef` | `#CDEAF7` 로고 하늘색 | 배경용 |
| `--sand` | `#d9c89e` | `#F0C43C` **로고 골드** | 배경용 |
| `--page` 뒷배경 | `#dff4f3` | `#E8F4FB` | — |

전부 `admin → 🎨테마` 에서 개별로 다시 바꿀 수 있습니다.

### summer-pose-03 교체
받은 파일로 바꿨습니다. 두 개 다 규격이 맞아 그대로 넣었습니다.
- `assets/summer-pose-03.png` (3840×2160)
- `assets/poster/summer-pose-03.webp` (1080×1440, 가로 중앙 정렬)

---

## 16. 글자 크기 조절 (🔤 글자크기 탭)

`admin → 🔤 글자크기` 에서 8개 항목을 **배율**로 조절합니다.
`1` 이 기본, `1.2` 면 20% 큼, `0.85` 면 15% 작음. 슬라이더로 끌거나 숫자를 직접 넣으면 됩니다.

| 항목 | 어디에 적용되나 |
|---|---|
| 전체 | 아래 모든 항목에 곱해집니다 |
| 히어로 대문짝 | 메인 첫 화면의 큰 KIMFP |
| 화면 제목 | 누적공약·방셀보상·굿즈보상·한정의상 |
| 소제목 | 확정 방셀 단가, 굿즈 단계명, 의상 카드 제목, 포스터 이름 |
| 영문 라벨 | CUMULATIVE PROMISE·MINI 같은 작은 영문 |
| 본문 설명 | 화면 설명문, 굿즈 구성, 공약 내용, 룰렛 항목 |
| 숫자·강조 | 582개·1111·10만 같은 큰 숫자 |
| 목차·버튼 | 왼쪽 목차, 탭 버튼, 목차 카드 |

오른쪽 **미리보기**에 즉시 반영되고, 저장하면 사이트 전체에 적용됩니다.
**기본값으로** 버튼으로 전부 `1` 로 되돌릴 수 있습니다.

### 반응형은 그대로 유지됩니다
기존 크기를 지우고 새 값을 넣는 방식이 아니라, **원래 값에 배율을 곱하는** 방식입니다.

```css
font-size: calc((clamp(58px, 7vw, 78px)) * var(--fs-hero, 1) * var(--fs-all, 1));
```

화면이 좁아지면 자동으로 줄어드는 동작(`clamp`·미디어쿼리)이 살아있고, 거기에 배율만
얹힙니다. 총 **92곳**이 이렇게 감싸져 있습니다.

> 배율이 전부 `1` 일 때 5개 화면 모두 **이전과 픽셀 단위로 완전히 동일**함을 확인했습니다.
